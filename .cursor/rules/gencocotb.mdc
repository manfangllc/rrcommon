---
description: 
globs: 
alwaysApply: true
---
---
description: 
globs: 
alwaysApply: false
---

   a) Imports and Setup:
      - Import required cocotb modules:
        ```python
        import cocotb
        from cocotb.clock import Clock
        from cocotb.triggers import Timer, RisingEdge, FallingEdge
        from cocotb.log import SimLog
        import random
        import traceback
        ```
      - Define test parameters at the top:
        ```python
        # Test parameters
        CLK_PERIOD = 10  # 10ns = 100MHz
        INIT_DELAY = 50  # Initial delay to allow signals to settle
        RESET_DELAY = 50  # Reset hold time
        # Add other module-specific parameters
        ```
      - Set up module-level logger:
        ```python
        log = SimLog("<module_name>_test")
        ```

   b) Helper Functions:
      - Create helper functions for common operations
      - Each helper should include:
        - Try-except block with error logging
        - Debug level logging for detailed operations
        - Clear error messages with stack traces
        - Return values or status as needed
      - Include waveform marker helpers:
        ```python
        async def add_wave_marker(dut, name):
            """Add a marker to the waveform for debugging."""
            log.debug(f"Adding waveform marker: {name}")
            await Timer(1, units="ns")  # Small delay for visibility
        ```

   c) Test Functions:
      - Each test should be decorated with @cocotb.test()
      - Use async/await for timing control
      - Follow this structure:
        ```python
        @cocotb.test()
        async def test_<module_name>_<test_case>(dut):
            """Test description."""
            try:
                log.info("Starting test")
                
                # Initial delay for signal settling
                await Timer(INIT_DELAY, units="ns")
                
                # Clock setup
                clock = Clock(dut.clk_i, CLK_PERIOD, units="ns")
                cocotb.start_soon(clock.start(start_high=False))
                
                # Reset sequence with waveform markers
                log.info("Applying reset")
                await add_wave_marker(dut, "Reset Start")
                dut.rst_n_i.value = 0
                await Timer(RESET_DELAY, units="ns")
                await add_wave_marker(dut, "Reset End")
                dut.rst_n_i.value = 1
                await Timer(CLK_PERIOD, units="ns")
                
                # Test specific operations
                # Use log.debug() for detailed operations
                # Use log.info() for major state changes
                # Add waveform markers for key events
                
                log.info("Test passed")
            except Exception as e:
                error_msg = f"Test failed: {str(e)}\n{traceback.format_exc()}"
                log.error(error_msg)
                raise
        ```

   d) Logging Guidelines:
      - Use log.debug() for detailed operations and signal changes
      - Use log.info() for major state changes and test progress
      - Use log.error() for test failures with full stack traces
      - Include relevant signal values in error messages
      - Format error messages to show expected vs actual values
      - Log signal values before and after critical operations
      - Include timing information in log messages

   e) Test Coverage:
      - Include reset state verification
      - Test basic functionality
      - Test edge cases and error conditions
      - Test multiple operations in sequence
      - Use random values where appropriate
      - Verify all outputs and state changes
      - Test timing requirements (setup/hold)
      - Verify parameter propagation
      - Test reset during active operation

   f) Error Handling:
      - Wrap all test functions in try-except blocks
      - Include full stack traces in error messages
      - Log all relevant signal values on failure
      - Use clear, descriptive error messages
      - Raise exceptions to fail tests
      - Include timing information in error messages
      - Log signal values before and after failures

   g) Timing Control:
      - Use Timer for fixed delays
      - Use RisingEdge/FallingEdge for clock-synchronized operations
      - Wait appropriate times for reset and state changes
      - Consider clock period in timing calculations
      - Add small delays (1ns) after clock edges for signal propagation
      - Use half-clock period delays for mid-cycle changes
      - Document timing requirements in test comments
      - Consider clock-to-Q delays in assertions

   h) Test Organization:
      - Group related tests together
      - Use clear, descriptive test names
      - Include docstrings explaining test purpose
      - Order tests from basic to complex
      - Reuse helper functions for common operations
      - Structure tests in increasing complexity:
        - Basic functionality (reset, clock)
        - Data capture
        - Hold behavior
        - Edge cases (reset during operation)
      - Each test should be independent and self-contained
      - Document test dependencies if any exist

   i) Parameter Handling:
      - Always ensure testbench parameters match DUT parameters
      - Define test parameters at the top of the file
      - Document the relationship between testbench and DUT parameters
      - Verify parameter passing in Makefile (PLUSARGS/COMPILE_ARGS for Icarus)
      - For parameterized modules, test with both default and non-default parameter values
      - Log parameter values at test start

   j) Reset Testing:
      - Verify asynchronous reset assertion (immediate effect)
      - Test reset hold behavior
      - Verify reset deassertion timing relative to clock
      - Test reset value propagation (all bits for multi-bit signals)
      - Include reset during active operation tests
      - Document expected reset timing in test comments
      - Add waveform markers for reset events
      - Test reset timing requirements

   k) Signal Timing:
      - Include small delays (1ns) after clock edges for signal propagation
      - Use half-clock period delays for mid-cycle changes
      - Test multiple clock cycles for setup/hold verification
      - Document timing requirements in test comments
      - Consider clock-to-Q delays in assertions
      - Add waveform markers for timing verification
      - Log timing information for critical operations

   l) Waveform Generation:
      - Configure waveform dumping in Makefile:
        ```makefile
        WAVES=1
        SIM_ARGS += --vcd=sim_build/$(TOPLEVEL).vcd
        SIM_ARGS += --fst=sim_build/$(TOPLEVEL).fst
        ```
      - Add waveform markers for key events
      - Use consistent timing for signal transitions
      - Document waveform markers in test comments
      - Verify waveform file generation
      - Use FST format for GTKWave compatibility

6. Create a Makefile in the module directory with the following structure:
# SystemVerilog Testbench Rules (Cocotb Focus)

### 🔧 Makefile Configuration
```makefile
# Makefile for module testbench

# Build directory setup (MUST BE AT TOP)
SIM_BUILD := sim_build
$(shell mkdir -p $(SIM_BUILD))

# Simulator selection
SIM ?= icarus
TOPLEVEL_LANG ?= verilog

# Source files
VERILOG_SOURCES = $(shell pwd)/module.sv

# Top level module
TOPLEVEL = module_name

# Test module
MODULE = test_module

# Enable SystemVerilog support
COMPILE_ARGS += -g2012

# Enable waveform dumping
WAVES=1
COMPILE_ARGS += -DWAVES=1

# Simulator output configuration
COCOTB_RESULTS_FILE := $(SIM_BUILD)/results.xml
VLOG_LOG_FILE := $(SIM_BUILD)/vlog_compile.log
VSIM_LOG_FILE := $(SIM_BUILD)/vsim_run.log
SIM_ARGS += -l$(SIM_BUILD)/sim.log

# Python path for cocotb
export PYTHONPATH := $(shell pwd):$(PYTHONPATH)

# Force simulation to run
.PHONY: sim
sim: $(SIM_BUILD)
	$(MAKE) -C $(SIM_BUILD) -f ../Makefile

# Additional targets for clean-up
.PHONY: clean_waves
clean_waves:
	rm -rf $(SIM_BUILD)/*.vcd

.PHONY: clean_all
clean_all: clean clean_waves
	rm -rf $(SIM_BUILD)

# Include cocotb makefiles
include $(shell cocotb-config --makefiles)/Makefile.sim
```

### 🔍 Common Build Issues and Solutions:
1. Missing sim_build directory:
   - Ensure SIM_BUILD is defined at top of Makefile
   - Use $(shell mkdir -p $(SIM_BUILD)) before any commands

2. Log file issues:
   - Define all log files within SIM_BUILD
   - Use -l flag for simulator log redirection
   - Avoid using > for output redirection

3. Python path issues:
   - Export PYTHONPATH explicitly
   - Include current directory in path

4. Clean-up issues:
   - Remove entire build directory in clean_all
   - Use proper directory references with $(SIM_BUILD)

5. Simulation control:
   - Include dedicated 'sim' target
   - Use proper -C flag for directory changes
   - Reference Makefile correctly with relative path

### 📝 Important Notes:
1. Build directory (SIM_BUILD) must be defined at the top of the Makefile
2. Use explicit directory creation with $(shell mkdir -p $(SIM_BUILD))
3. Export PYTHONPATH to ensure proper module discovery
4. Use := instead of ?= for build-critical variables
5. Define all log files within SIM_BUILD directory
6. Include dedicated 'sim' target for better control
7. Clean targets should remove entire build directory

### 🚀 Quick Start:
1. Copy the Makefile template above
2. Replace module.sv with your SystemVerilog file name
3. Replace module_name with your top-level module name
4. Replace test_module with your Python test file name (without .py)
5. Run `make clean_all && make sim` to build and run tests